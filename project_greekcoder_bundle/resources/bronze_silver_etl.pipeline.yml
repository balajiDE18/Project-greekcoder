# The main pipeline for project_greekcoder_bundle

resources:
  pipelines:
    bronze_silver_etl:
      name: bronze_silver
      configuration:
        catalog_name: ${var.catalog}
      libraries:
        - glob:
            include: ../src/DLT_pipelines/bronze_silver/transformations/**
      schema: silver
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${var.catalog}
      serverless: true
      root_path: ../src/DLT_pipelines/bronze_silver


