 # This is a Databricks asset bundle definition for project_greekcoder_bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: project_greekcoder_bundle
  uuid: 38a59a37-24af-4ced-b82a-83616d29f4a2

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use
  warehouse_id:
    description: The warehouse_id to use
targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-1a55a510-be18.cloud.databricks.com
    variables:
      catalog: dev_catalog
      schema: dev
      warehouse_id: 78835c2d2de3503e
  prod:
    mode: production
    workspace:
      host: https://dbc-1a55a510-be18.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/balajigcp18@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/balajigcp18@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dev_catalog
      schema: prod
    permissions:
      - level: CAN_MANAGE  
        user_name: balajigcp18@gmail.com
        
